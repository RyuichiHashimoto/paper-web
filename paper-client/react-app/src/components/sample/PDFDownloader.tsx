// App.js

import React, { useState } from 'react';

const DEMO = () => {
    const [showModal, setShowModal] = useState(false); // Modalコンポーネントの表示の状態を定義する

    return (
        <>
            <h2>Modal実装</h2>
        </>
    );
};

export default DEMO;